// a simple condition
const age = 12;

if(age >= 18){
    console.log('You are corect');
}else{
    console.log('You are not eligible');
}

// it's simplified using ternary operator
const check = (age >= 18) ? 'oook' : 'Not';
console.log(check);


// simplifies true false outcome
const roll = 10;

const out = roll > 5;

console.log(out);